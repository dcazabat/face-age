[project]
name = "face-age"
version = "0.1.0"
description = "Reconocimiento por camara con edad, genero y emocion aproximados"
requires-python = ">=3.10"
dependencies = [
    "opencv-python>=4.10.0",
    "numpy>=2.0.0",
    "flask>=3.0.0",
]

[tool.uv]
package = false
